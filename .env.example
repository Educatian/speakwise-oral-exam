# ═══════════════════════════════════════════════════════════════════════════
# SpeakWise Environment Variables
# ═══════════════════════════════════════════════════════════════════════════
# Copy this file to .env and fill in your values

# ─────────────────────────────────────────────────────────────────────────────
# Gemini API (Required for AI voice interviews)
# Get your key at: https://aistudio.google.com/apikey
# ─────────────────────────────────────────────────────────────────────────────
GEMINI_API_KEY=your_gemini_api_key_here

# ─────────────────────────────────────────────────────────────────────────────
# Supabase Configuration (Required for cloud database)
# Get these values from: https://supabase.com
# 1. Create a project
# 2. Go to Project Settings > API
# 3. Copy the Project URL and anon/public key
# ─────────────────────────────────────────────────────────────────────────────
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ─────────────────────────────────────────────────────────────────────────────
# Supabase Setup Instructions:
# ─────────────────────────────────────────────────────────────────────────────
# 
# 1. Go to https://supabase.com and create an account (or login)
# 
# 2. Click "New Project" and fill in:
#    - Project name: speakwise
#    - Database password: (save this!)
#    - Region: Choose closest to you
# 
# 3. Wait for project to be created (1-2 minutes)
# 
# 4. Go to Project Settings (gear icon) > API
#    - Copy "Project URL" → VITE_SUPABASE_URL
#    - Copy "anon public" key → VITE_SUPABASE_ANON_KEY
# 
# 5. Go to SQL Editor (left sidebar) and run:
# 
# -- Courses table
# CREATE TABLE courses (
#   id TEXT PRIMARY KEY,
#   name TEXT NOT NULL,
#   password TEXT NOT NULL,
#   prompt TEXT NOT NULL,
#   created_at TIMESTAMPTZ DEFAULT NOW()
# );
# 
# -- Submissions table
# CREATE TABLE submissions (
#   id TEXT PRIMARY KEY,
#   course_id TEXT REFERENCES courses(id) ON DELETE CASCADE,
#   student_name TEXT NOT NULL,
#   course_name TEXT,
#   timestamp BIGINT NOT NULL,
#   transcript JSONB NOT NULL DEFAULT '[]',
#   score INTEGER NOT NULL DEFAULT 0,
#   feedback TEXT,
#   created_at TIMESTAMPTZ DEFAULT NOW()
# );
# 
# -- Student history table
# CREATE TABLE student_history (
#   id TEXT PRIMARY KEY,
#   device_id TEXT NOT NULL,
#   student_name TEXT NOT NULL,
#   course_name TEXT,
#   timestamp BIGINT NOT NULL,
#   transcript JSONB NOT NULL DEFAULT '[]',
#   score INTEGER NOT NULL DEFAULT 0,
#   feedback TEXT,
#   created_at TIMESTAMPTZ DEFAULT NOW()
# );
# 
# -- Enable RLS and allow public access (for development)
# ALTER TABLE courses ENABLE ROW LEVEL SECURITY;
# ALTER TABLE submissions ENABLE ROW LEVEL SECURITY;
# ALTER TABLE student_history ENABLE ROW LEVEL SECURITY;
# 
# CREATE POLICY "Allow all" ON courses FOR ALL USING (true);
# CREATE POLICY "Allow all" ON submissions FOR ALL USING (true);
# CREATE POLICY "Allow all" ON student_history FOR ALL USING (true);
# 
# 6. Enable Realtime for tables (optional but recommended):
#    - Go to Database > Replication
#    - Enable for courses and submissions tables
# 
# 7. Restart your dev server: npm run dev
